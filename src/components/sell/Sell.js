import React from 'react'
import Header from '../Header'

const Sell = (props) => {
    return (
        <div>
            <Header pageName={props.pageName}/>
        </div>
    )
}

export default Sell

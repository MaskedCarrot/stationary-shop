import React from 'react'
import Header from '../Header'

const Report = (props) => {
    return (
        <div>
            <Header pageName={props.pageName}/>
        </div>
    )
}

export default Report
